# Maven build output
target/

# Local credentials for docker compose. .env.example is the committed template; .env never is.
.env

# IDE and editor noise
.idea/
*.iml
.vscode/

# OS and OneDrive noise
.DS_Store
Thumbs.db
desktop.ini

# Python bytecode from the evaluation scripts
__pycache__/

# TLC's jar download and its scratch state
spec/.tools/
spec/states/
spec/*_TTrace_*

# Every v2 score with its explanation, 5 MB, rebuilt by eval/fraud/run.py
eval/fraud/scores-v2.jsonl
