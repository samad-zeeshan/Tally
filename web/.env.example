# Copy this file to .env.local and set the token to the backend's dev token (TALLY_API_TOKEN).
# Anything in VITE_* is baked into the served JavaScript and readable by anyone who opens the page, so
# this is a demo-only credential for a local backend, never a real secret.
VITE_API_TOKEN=
